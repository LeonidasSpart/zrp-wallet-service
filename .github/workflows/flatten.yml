name: Flatten SDK

permissions:
  contents: write

on:
  workflow_dispatch:

jobs:
  flatten:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Flatten directory
        run: |
          if [ -d "zrp-wallet-service-main" ]; then
            shopt -s dotglob
            mv zrp-wallet-service-main/* .
            rmdir zrp-wallet-service-main
          else
            echo "zrp-wallet-service-main not found, nothing to flatten"
            exit 1
          fi

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"
          git add -A
          git diff --staged --quiet || git commit -m "Flatten repo: move SDK files to root"
          git push
